// UI Icons (keep Lucide for clean UI)
import {
  Mail,
  Phone,
  MapPin,
  ArrowUpRight,
  ExternalLink,
  FileText,
} from "lucide-react";

// Brand Icons (colored)
import {
  SiGithub,
} from "react-icons/si";

import { FaLinkedinIn } from "react-icons/fa";


// Export grouped icons
export const Icons = {
  // UI
  Mail,
  Phone,
  MapPin,
  ArrowUpRight,
  ExternalLink,
  FileText,

  // Brands (colored)
  Github: SiGithub,
  Linkedin: FaLinkedinIn,
};